import { createTheme } from "@mui/material/styles";

export const linkTheme = createTheme({
  palette: {
    primary: {
      main: "#5A42ED",
    },
    secondary: {
      main: "#edf2ff",
    },
    accent: {
      main: "#C8553D",
    },
  },
  typography: {
    fontSize: 17,
  },
});
