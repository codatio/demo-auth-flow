import { createTheme } from '@mui/material/styles';

export const linkTheme = createTheme({
  palette: {
    primary: {
      main: '#5D9E8F',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});