import { createTheme } from '@mui/material/styles';

export const linkTheme = createTheme({
  palette: {
    primary: {
      main: '#4E4C67',
    },
    secondary: {
      main: '#edf2ff',
    },
    accent: {
      main: '#C8553D',
    },
  },
});
